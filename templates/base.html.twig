<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}SkinMarket{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    {{ importmap('app') }}

    <style>
        body {
            background-color: #121212;
            color: #f8f9fa;
        }

        nav {
            background-color: #1e1e1e;
            border-bottom: 2px solid #0dcaf0;
        }

        a.nav-link {
            color: #f8f9fa !important;
            transition: 0.3s;
        }

        a.nav-link:hover {
            color: #0dcaf0 !important;
        }

        .navbar-brand {
            color: #0dcaf0 !important;
            font-weight: bold;
        }

        .alert {
            transition: opacity 0.5s ease-in-out;
            opacity: 0;
            pointer-events: none;
        }

        .alert.show {
            opacity: 1;
            pointer-events: auto;
        }

        .dropdown-menu {
            background-color: #212529;
            border: 1px solid #0dcaf0;
        }

        .dropdown-item:hover {
            background-color: #0dcaf0;
            color: #000 !important;
        }
    </style>
</head>

<body data-controller="cart">
<nav class="navbar navbar-expand-lg navbar-dark mb-4 shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">🌙 SkinMarket</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Liens principaux -->
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_shop_index') }}">🛍️ Boutique</a>
                </li>
                {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product_index') }}">📦 Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_category_index') }}">📚 Catégories</a>
                    </li>
                {% endif %}
            </ul>

            <!-- Panier dynamique + Auth -->
            <ul class="navbar-nav align-items-center">
                <li class="nav-item position-relative me-3">
                    <a href="{{ path('app_shop_cart') }}" class="nav-link fw-bold text-info">
                        🧺 <span data-cart-target="count">{{ app.session.get('cart')|length }}</span>
                    </a>
                    <small class="text-muted ms-1" data-cart-target="total">0.00 €</small>
                </li>

                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userMenu" role="button" data-bs-toggle="dropdown">
                            👤 {{ app.user.email }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <li>
                                    <a class="dropdown-item text-light" href="{{ path('app_product_index') }}">Admin Dashboard</a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                            {% endif %}
                            <li>
                                <a class="dropdown-item text-light" href="{{ path('app_logout') }}">🚪 Déconnexion</a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<!-- Messages flash -->
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} text-center fw-bold position-fixed top-0 start-50 translate-middle-x mt-3 show"
             style="z-index: 1050;">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

<!-- Message du panier dynamique -->
<div id="flash-message"
     class="alert alert-success position-fixed top-0 start-50 translate-middle-x mt-3 text-center fw-bold"
     style="display:none; z-index: 2000;"
     data-cart-target="message">
</div>

<main class="container mb-5">
    {% block body %}{% endblock %}
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
