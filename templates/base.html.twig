<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}SkinMarket{% endblock %}</title>

    {# ✅ Bootstrap Dark Theme (CDN) #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #0e0e0e;
            color: #e0e0e0;
            font-family: 'Segoe UI', sans-serif;
        }
        nav.navbar {
            background-color: #1a1a1a !important;
            border-bottom: 1px solid #333;
        }
        nav a.nav-link {
            color: #f1f1f1 !important;
            transition: color 0.3s;
        }
        nav a.nav-link:hover {
            color: #ffcc00 !important;
        }
        .btn-primary {
            background-color: #ffcc00;
            border: none;
            color: #000;
            font-weight: bold;
        }
        .btn-primary:hover {
            background-color: #e6b800;
        }
        footer {
            border-top: 1px solid #333;
            text-align: center;
            padding: 20px;
            color: #777;
            margin-top: 60px;
        }
    </style>
</head>

<body>
    {# 🌙 Barre de navigation #}
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand text-warning fw-bold" href="{{ path('app_home') }}">💀 SkinMarket</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_product_index') }}">Produits</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    <footer>
        <p>© 2025 SkinMarket — Skins CS:GO Marketplace</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
