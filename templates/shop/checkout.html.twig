{% extends 'base.html.twig' %}

{% block title %}Paiement sécurisé{% endblock %}

{% block body %}
<div class="container mt-5 text-light">
    <div class="card bg-dark border-info shadow-lg p-5 mx-auto" style="max-width: 700px;">
        <h2 class="text-center text-info mb-4 fw-bold">💳 Paiement sécurisé</h2>
        <p class="text-center text-muted">Veuillez saisir vos informations de paiement pour finaliser la commande</p>

        <form method="post" novalidate>
            <div class="mb-3">
                <label class="form-label">Numéro de carte</label>
                <input type="text" 
                       name="card_number"
                       pattern="\d{16}" 
                       maxlength="16"
                       class="form-control bg-dark text-light border-info"
                       placeholder="1111 2222 3333 4444" 
                       required>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">Date d’expiration (MM/AA)</label>
                    <input type="text" 
                           name="expiration" 
                           pattern="\d{2}/\d{2}"
                           maxlength="5"
                           class="form-control bg-dark text-light border-info"
                           placeholder="12/27" 
                           required>
                </div>
                <div class="col-md-6 mb-3">
                    <label class="form-label">CVV</label>
                    <input type="text" 
                           name="cvv"
                           pattern="\d{3}"
                           maxlength="3"
                           class="form-control bg-dark text-light border-info"
                           placeholder="123" 
                           required>
                </div>
            </div>

            <div class="text-center mt-4">
                <button type="submit" class="btn btn-info px-5 py-2 fw-bold rounded-pill shadow-sm">💰 Payer maintenant</button>
            </div>
        </form>

        <hr class="border-info my-4">

        <h5 class="text-center text-info">🧾 Récapitulatif</h5>
        <ul class="list-group bg-dark border-0">
            {% for item in items %}
                <li class="list-group-item bg-dark text-light border-info d-flex justify-content-between">
                    <span>{{ item.product.name }} × {{ item.quantity }}</span>
                    <strong>{{ item.subtotal }} €</strong>
                </li>
            {% endfor %}
            <li class="list-group-item bg-info text-dark d-flex justify-content-between fw-bold">
                <span>Total</span>
                <span>{{ total }} €</span>
            </li>
        </ul>
    </div>
</div>

<style>
input:focus {
    border-color: #0dcaf0 !important;
    box-shadow: 0 0 10px rgba(13, 202, 240, 0.5);
}
button:hover {
    transform: scale(1.05);
    transition: 0.2s;
}
</style>
{% endblock %}
