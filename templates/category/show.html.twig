{% extends 'base.html.twig' %}

{% block title %}{{ 'category.show_title'|trans }}{% endblock %}

{% block body %}
<div class="container mt-5 text-light">
    <div class="hero-section mb-4 p-4 p-lg-5 rounded">
        <p class="badge bg-info text-dark mb-2">#Catégorie</p>
        <h1 class="fw-bold mb-2">📁 {{ 'category.show_title'|trans }}</h1>
        <p class="text-muted mb-0">Détails et produits associés.</p>
    </div>

    <div class="card bg-dark border-info shadow-lg p-4">
        <table class="table table-dark table-striped align-middle">
            <tbody>
                <tr>
                    <th class="text-info">ID</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th class="text-info">{{ 'product.name'|trans }}</th>
                    <td class="fw-bold">{{ category.name }}</td>
                </tr>
            </tbody>
        </table>

        {% if category.products is defined and category.products|length > 0 %}
            <h3 class="text-warning mt-4 mb-3">🧩 {{ 'category.products_title'|trans }}</h3>
            <ul class="list-group list-group-flush">
                {% for product in category.products %}
                    <li class="list-group-item bg-dark text-light border-secondary d-flex justify-content-between align-items-center">
                        <span>{{ product.name }}</span>
                        <a href="{{ path('app_product_show', {'id': product.id}) }}" class="btn btn-outline-info btn-sm">👁️ {{ 'common.view'|trans }}</a>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p class="text-muted mt-3 fst-italic">{{ 'category.no_products'|trans }}</p>
        {% endif %}

        <div class="text-center mt-4">
            <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-warning btn-lg me-2">✏️ {{ 'product.edit'|trans }}</a>
            <a href="{{ path('app_category_index') }}" class="btn btn-outline-light btn-lg">↩️ {{ 'common.back'|trans }}</a>
        </div>
    </div>
</div>
<style>
.hero-section {
    background: radial-gradient(circle at 20% 20%, rgba(13,202,240,0.12), transparent 30%), #0f131a;
    border: 1px solid rgba(13,202,240,0.2);
    box-shadow: 0 12px 30px rgba(0,0,0,0.35);
}
</style>
{% endblock %}
