{% extends 'base.html.twig' %}

{% block title %}Créer un compte{% endblock %}

{% block body %}
<div class="min-vh-100 d-flex align-items-center justify-content-center bg-dark text-light">
    <div class="card bg-dark border-info shadow-lg p-4 w-100" style="max-width: 550px;">
        <div class="mb-3 text-center">
            <p class="badge bg-info text-dark mb-2">#Inscription</p>
            <h2 class="text-info fw-bold mb-1">🧾 Créer un compte</h2>
            <p class="text-muted mb-0 small">Nom, prénom et adresse de facturation requis.</p>
        </div>

        {{ form_start(registrationForm) }}
            <div class="mb-3">
                {{ form_label(registrationForm.email, 'Adresse e-mail', {'label_attr': {'class': 'form-label text-light'}}) }}
                {{ form_widget(registrationForm.email, {'attr': {'class': 'form-control bg-dark text-light border-info'}}) }}
                {{ form_errors(registrationForm.email) }}
            </div>

            <div class="mb-3">
                {{ form_label(registrationForm.firstName, 'Prénom', {'label_attr': {'class': 'form-label text-light'}}) }}
                {{ form_widget(registrationForm.firstName, {'attr': {'class': 'form-control bg-dark text-light border-info'}}) }}
                {{ form_errors(registrationForm.firstName) }}
            </div>

            <div class="mb-3">
                {{ form_label(registrationForm.lastName, 'Nom', {'label_attr': {'class': 'form-label text-light'}}) }}
                {{ form_widget(registrationForm.lastName, {'attr': {'class': 'form-control bg-dark text-light border-info'}}) }}
                {{ form_errors(registrationForm.lastName) }}
            </div>

            <div class="mb-3">
                {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'form-label text-light'}}) }}
                {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'form-control bg-dark text-light border-info'}}) }}
                {{ form_errors(registrationForm.plainPassword) }}
            </div>

            <div class="border border-info rounded p-3 mb-3">
                <h5 class="text-info">Adresse de facturation</h5>
                {{ form_row(registrationForm.address.street, {'label': 'Rue et numéro', 'attr': {'class': 'form-control bg-dark text-light border-info mb-2'}}) }}
                {{ form_row(registrationForm.address.postalCode, {'label': 'Code postal', 'attr': {'class': 'form-control bg-dark text-light border-info mb-2'}}) }}
                {{ form_row(registrationForm.address.city, {'label': 'Ville', 'attr': {'class': 'form-control bg-dark text-light border-info mb-2'}}) }}
                {{ form_row(registrationForm.address.country, {'label': 'Pays', 'attr': {'class': 'form-control bg-dark text-light border-info mb-0'}}) }}
            </div>

            <div class="form-check mb-3">
                {{ form_widget(registrationForm.agreeTerms, {'attr': {'class': 'form-check-input'}}) }}
                {{ form_label(registrationForm.agreeTerms, 'J’accepte les conditions', {'label_attr': {'class': 'form-check-label text-light'}}) }}
            </div>

            <button type="submit" class="btn btn-info w-100 fw-bold">Créer mon compte</button>
        {{ form_end(registrationForm) }}

        <div class="text-center mt-3">
            <a href="{{ path('app_login') }}" class="text-decoration-none text-light">Déjà un compte ? Connecte-toi</a>
        </div>
    </div>
</div>
{% endblock %}
